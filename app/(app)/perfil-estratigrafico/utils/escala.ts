/** Pixels por metro na horizontal (posição dos furos). */
export const ESCALA_X = 8;

/** Pixels por metro na vertical (cota / profundidade). */
export const ESCALA_Y = 40;

/** Largura fixa do SVG do perfil (px). */
export const SVG_LARGURA_PERFIL_PX = 1200;
