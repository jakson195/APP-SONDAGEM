# Movido para `api-geospatial/`

O backend FastAPI do Digital Twin foi extraído para o módulo **`../../api-geospatial/`** (Fase 2 da arquitetura modular).

- Desenvolvimento: `docker compose` na raiz do repositório (`api-geospatial` + `postgres-geo`)
- Stack legado local: atualize `digital-twin/docker-compose.yml` para apontar a `../api-geospatial` se ainda usar este diretório

Não adicione código novo aqui.
