import { MarketingHome } from "@/components/marketing/marketing-home";

/** Landing comercial na raiz (/) — layout `(site)` aplica MarketingShell. */
export default function SiteHomePage() {
  return <MarketingHome />;
}
