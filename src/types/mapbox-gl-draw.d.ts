/**
 * Fallback mínimo se @types/mapbox__mapbox-gl-draw não estiver em node_modules.
 * Com o pacote @types instalado, este ficheiro funde-se com as definições oficiais.
 */
declare module "@mapbox/mapbox-gl-draw";
